using System;

namespace Sandbox;

public fun void PrintMath()
{
	/*
	Console::WriteLine("Add: %i", Math::Add(2, 2));
	Console::WriteLine("Sub: %i", Math::Sub(2, 2));
	Console::WriteLine("Mul: %i", Math::Mul(2, 2));
	Console::WriteLine("Div: %i", Math::Div(2, 2));
	Console::WriteLine("Mod: %i", Math::Mod(2, 2));
	*/
}