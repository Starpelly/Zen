using System;

namespace Sandbox; // Compiler breaks without namespace. This is a bug!

public fun void Main()
{
	let string constant = "Hello from Zen!!!!"; // Immutable variable, the value cannot be changed.
	var int mutable = 0;						// Mutable variable, the value can be changed.
	
	// constant = "what";

	Console::WriteLine(constant);
}

private fun bool typeCheck()
{
	let bool returnValue = true;

	return returnValue;
}