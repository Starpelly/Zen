namespace Raylib;

public struct Color
{
    public var uint8 r;
    public var uint8 g;
    public var uint8 b;
    public var uint8 a;
    
    public fun self(let uint8 r, let uint8 g, let uint8 b, let uint8 a)
    {
        cembed("""
        return CLITERAL(Raylib_Color) { r, g, b, a };
        """);
        // self.r = r;
        // self.g = g;
        // self.b = b;
        // self.a = a;
    }
}

public struct Vector2
{
    public var float x;
    public var float y;

    public fun self(let float x, let float y)
    {
        cembed("""
        return CLITERAL(Raylib_Vector2) { x, y };
        """);
    }
}