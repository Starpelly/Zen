namespace Pelly;

public struct Player
{
    public var Raylib::Vector2 position;
    public var Raylib::Vector2 size;

    public function this()
    {
        this.size = Raylib::Vector2(32, 32);
    }
}