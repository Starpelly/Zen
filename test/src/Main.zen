// using System;
using Pepsi;
using DrPepper;

namespace Sandbox;

cembed("#include <stdio.h>");

public fun void Main()
{
	// Console::WriteLine("Hello, Sailor!");
	// Console::TestMessageBox("Hello, World!", "Title");

	// RunGame();

	let Pepsi::Color col = Pepsi::Color::Red;

	let int fib = Sandbox::Fibonacci(30);
	cembed("""
	printf("%i\n", fib);
	""");
}

public fun int Fibonacci(let int num)
{
	if (num < 2)
	{
		return num;
	}
	else
	{
		return Sandbox::Fibonacci(num - 1) + Sandbox::Fibonacci(num - 2);
	}
}