namespace Program;

fun void Hello()
{
	// Comments aren't included in the output.

	printf("hello from zen!\n");
}

fun void PrintMath()
{
	printf("%i\n", Add(2, 2));
	printf("%i\n", Sub(2, 2));
	printf("%i\n", Mult(2, 2));
	printf("%i\n", Div(2, 2));
}