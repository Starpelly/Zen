using System;

namespace Sandbox;

public fun void Main()
{
	let string_view constant = "Hello from Zen!!!";
	var int mutable = 0;
	let bool boolean = false;

	Console::WriteLine(constant);

	let bool typeChecker = typeCheck(true);
}

private fun bool typeCheck(let bool print)
{
	let bool returnValue = true;

	// print = false;
	if (print)
	{
		let string_view soTrue = "This is fucking true bro!!!!";
		Console::WriteLine(soTrue);
	}

	cembed
	{
		printf("%s\n", (print) ? "true" : "false");
	}

	return returnValue;
}